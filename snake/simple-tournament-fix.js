// Simplified Tournament Auto-Advance Version
// This removes all evolution overlay complexity and just auto-advances the tournament

// Remove all references to showEvolutionResults and feeding mode overlays
// Just advance tournament automatically after each match

// In the endGame function, simply advance the tournament directly:

function simplifyTournamentProgression() {
    // Tournament matches auto-advance with a brief delay
    // No evolution overlays or feeding mode displays
    // Just show winner message and proceed to next match
}
